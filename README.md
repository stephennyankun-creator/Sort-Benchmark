# Sort-Benchmark
Sorts the same randomly generated dataset using Bubble Sort, Insertion Sort, Merge Sort, and Quick Sort. The program compares each algorithm's runtime, number of comparisons, and swaps or shifts side by side.
